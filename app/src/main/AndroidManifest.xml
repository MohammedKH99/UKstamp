<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alhalim.ukstamp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/m1332"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentHome" />
        <activity android:name=".AdminHome" />
        <activity android:name=".Sample" />
        <activity android:name=".Sample_quiz" />
        <activity android:name=".Animation_cources" />
        <activity android:name=".Available_cources" />
        <activity android:name=".LoginPage"></activity>
    </application>

</manifest>